---
interface Props {
    tableData: { heading: string; text: string }[];
    className?: string;
}

const { tableData, className = '' } = Astro.props;
---

<table class={`product-table ${className}`}>
    <tbody>
        {
            tableData.map((row) => (
                <tr>
                    <th class="py-5 px-5">{row.heading}</th>
                    <td class="py-5 px-5">{row.text}</td>
                </tr>
            ))
        }
    </tbody>
</table>

<style>
    .product-table {
        border-collapse: collapse;
        width: 100%;
        font-size: 0.95rem;
    }

    .product-table th,
    .product-table td {
        border: 1px solid #ccc;
        text-align: left;
        vertical-align: top;
    }

    .product-table th {
        background-color: #f5f5f5;
        font-weight: bold;
        width: 28%;
        white-space: nowrap;
    }

    .product-table td {
        background-color: #fff;
    }
</style>
